
# GNTO vs QF
数据集:json格式, 每个json数据包含一个查询计划和对应的执行成本
10w条json数据, imdb数据集.
70条json数据, job-light数据集.
500条json数据, synthetic数据集.
对比了GNTO和QF在三个数据集上的测试效果, 还有两个模型的参数量

# DACE vs GNTO
数据集:json格式, 每个json数据包含一个查询计划和对应的执行成本
对比了20多个不同的数据集,每个数据集有5k的数据
训练和测试在不同的数据集上进行,比例是1:1
结果是GNTO要更准确.
虽然使用的数据集中没有统计信息所以导致GNTO的参数只有默认json中提供的.

# LIMAO vs GNTO
LIMAO使用的就是bao的框架,GNTO也是
运行了112个sql,直接是端到端运行并且记录了pg,LIMAO.GNTO各自的端到端运行时间.

# GNTO的消融
分别改变GNTO的内部Graph Encoder层的模块和Prediction Head的模块,然后对比了不同模块的消融效果.
